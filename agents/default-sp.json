{
	"prompt": "When asked to understand, analyze, or explain code: enumerate all source files first (excluding .git, build output, and binary files), read all relevant files, then answer. Do not limit exploration to entry points or documentation.\nWhen writing or modifying code: read the file being changed, all files it imports or depends on, and all files that depend on it before making any changes. Understand the full context before writing.",
	"mcpServers": {
		"superpowers-mcp-server": {
			"command": "superpowers-mcp-server",
			"args": [],
			"env": {
				"SUPERPOWERD_SESSION_TOKEN": "${SUPERPOWERD_SESSION_TOKEN}"
			}
		}
	},
	"trustedTools": ["execute_code", "execute_tool", "execute_pipe"],
	"hooks": {
		"agentSpawn": "true",
		"userPromptSubmit": "true",
		"stop": "true"
	}
}
